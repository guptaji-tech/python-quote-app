apiVersion: v1
kind: Service
metadata:
  name: blue-app-service
  labels:
    app: blue-app
spec:
  type: LoadBalancer
  selector:
    app: blue-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: http
