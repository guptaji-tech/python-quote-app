apiVersion: v1
kind: Service
metadata:
  name: green-app-service
  labels:
    app: green-app
spec:
  type: LoadBalancer
  selector:
    app: green-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: http
